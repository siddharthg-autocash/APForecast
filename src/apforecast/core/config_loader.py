# src/apforecast/core/config_loader.py
# Vendor overrides were removed from runtime. Keeping a small stub to avoid import errors

import warnings

def load_vendor_overrides():
    warnings.warn("load_vendor_overrides() is deprecated/disabled — vendor overrides are currently removed.")
    return {}
